apiVersion: v1
kind: Service
metadata:
  labels:
    app: downstream-deployment
  name: yet-another-k8sshowcase
spec:
  ports:
    - port: 8080
      name: application
      protocol: TCP
      targetPort: 8080
  selector:
    app: downstream-deployment
